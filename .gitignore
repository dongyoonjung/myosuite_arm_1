# 데이터 — GCS 버킷에 보관(레포에 넣지 않음)
data/

# 학습 산출물
models/
logs/
*.zip
*.pkl

# 파이썬
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/

# OS
.DS_Store

# Claude Code 로컬 설정
.claude/

# 보고서 이미지/비디오 — 사용자 요청으로 제외(MD 본문은 커밋)
results/report/

# 중간 체크포인트(대용량) — 최종 모델만 force-add로 커밋
models/checkpoints/

# MuJoCo 렌더 로그
MUJOCO_LOG.TXT

# LaTeX aux
*.aux
*.out
*.synctex.gz
